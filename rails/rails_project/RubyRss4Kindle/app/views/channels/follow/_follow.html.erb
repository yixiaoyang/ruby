<%= form_for(current_user.follow_relationships.build(followed_id: @user.id), remote: true) do |f|  %>
<!--传送参数 :followed_id即@user.id, 动作是follow, ajax remote支持会多一个元素设置data-remote="true"-->
<%= f.hidden_field :followed_id %>
  <%= f.submit "follow", class: "btn btn-small btn-primary" %>
<% end%>
