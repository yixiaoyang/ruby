<% provide(:title, 'All Users')%>
<h1>All Users</h1>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
    <%= gravatar_for user%>
    <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
<!-- will_paginate的分页需要调用对象集合的paginate方法 -->
<%= will_paginate %>