<%= form_for(current_user.follow_relationships.build(followed_id: followed_user.id)) do |f|  %>
<!--传送参数 :followed_id即@user.id, 动作是follow-->
<%= f.hidden_field :followed_id %>
  <%= f.submit "follow", class: "btn btn-small btn-primary" %>
<% end%>
